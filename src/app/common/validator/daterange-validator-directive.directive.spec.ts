import { DaterangeValidatorDirectiveDirective } from './daterange-validator-directive.directive';

describe('DaterangeValidatorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DaterangeValidatorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
